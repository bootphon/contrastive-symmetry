size <- combine_dlist(read_from_filename_table(SIZE_FILENAME_TABLE))
sum_fbalance_spec <- combine_dlist(read_from_filename_table(SUM_FBALANCE_FILENAME_TABLE))
sum_fnpairs_spec <- combine_dlist(read_from_filename_table(SUM_FNPAIRS_FILENAME_TABLE))
extra_geometries <- read.csv(EXTRA_GEOMETRIES)
